import { InjectionToken } from '@angular/core';

export const ArticlesToken = new InjectionToken('This will provide all articles');
export const ArticleIndexToken = new InjectionToken('This will provide index to delete');
